# Docker Compose
# https://docs.docker.com/compose/
#
# Usage: test up

version: '3'
services:

  app:
    image: node-tweets-app-test:0.0.2
    environment: &environment
      - STAGE=test
    build:
      args:
        stage: test
        version: 0.0.2
    volumes:
      - .:/usr/src
